apiVersion: v1
kind: Service
metadata:
  name: demo-app-preview
  namespace: demo-app
  labels:
    app: demo-app
    role: canary
    managed-by: emmanuel-ubani
spec:
  selector:
    app: demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
